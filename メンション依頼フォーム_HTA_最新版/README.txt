メンション依頼フォーム｜本番実装候補 v1
========================================

■ 本番化した3本柱

① ローカル退避（データ消失対策）
送信ボタン押下後、最初にDesktop\MentionRequest_Pendingへ
可読形式の.pendingを保存します。

Pending保存後にだけBackground Workerを起動するため、
共有フォルダ障害やCSV競合が発生しても元データを保持します。

② Background Worker（処理中断対策）
CSV書き込みはHTAとは別のPowerShell workerで実行します。

受付完了後はHTAを×で閉じても処理継続。
成功後にWindows右下通知を表示します。

HTA次回起動時には残存Pendingを自動検出して再送します。

③ lock＋ランダム再試行（同時送信対策）
CSVごとに.lockを排他取得してから処理します。

lock方式はCreateNewではなく
OpenOrCreate + FileShare.None
へ変更しました。

このため、workerが異常終了して.lockファイル自体が残っても、
Windowsのhandleが解放されていれば再度lock取得可能です。
「15秒経過したlockを強制削除」のような危険な処理はありません。

再試行は450～1250msのランダム間隔です。


■ RequestID重複・途中書き込み対策

RequestIDだけで「送信済み」と判断せず、
RequestID＋依頼番号（依頼1/2/3）単位で確認します。

例：
依頼1・2・3を送信中にPC/workerが停止
↓
CSVには依頼1だけ反映
↓
次回Pending自動再送
↓
依頼1は既存としてスキップ
依頼2・3だけ追記
↓
3件すべて揃ったことを確認
↓
Pending削除

これにより、
「一部だけ書けた状態でRequestIDを重複扱いして残りが消える」
リスクを抑えています。


■ CSV書き込み確認

同じlockを保持したまま、
書き込み後にRequestID＋依頼番号を再読込し、
Pending内の全依頼番号がCSVに存在することを確認します。

不足があればPendingを削除せず再試行します。


■ CSVファイルの日付

復旧が翌日になっても、
Pendingを作成した日付のCSVへ書き込みます。

例：
9/3 23:59に送信 → 9/4に復旧
→ 呉服_20260903.csv


■ 送信先設定

mention-form.config.ini
のCSV_FOLDERへ本番共有フォルダを設定してください。

推奨：
\\サーバー名\共有フォルダ\メンション依頼

UNCパスを推奨します。

V3 Pendingには送信時点のCSV_FOLDERも保存するため、
送信後にconfig設定が変更されても、
既存Pendingは元の送信先へ復旧します。


■ Windows通知

通常成功：
メンション依頼｜送信完了

既に全件送信済み：
メンション依頼｜送信済み確認

途中書き込みから復旧：
メンション依頼｜送信完了
「途中まで反映済みの依頼を補完して…」

長時間待機：
メンション依頼｜自動再送中


■ ローカル診断ログ

%LOCALAPPDATA%\MentionRequest\Logs\worker_YYYYMMDD.log

START / RETRY / SUCCESS / DUPLICATE_SKIP /
RECOVERED_PARTIAL / FATAL 等を記録します。

ログ失敗は送信処理へ影響しません。


■ 本番前に残っている確認

・mention-form.config.iniへ実際の共有フォルダを設定
・職場PC 2台以上＋同じ共有フォルダで負荷テスト
・PowerShell実行可否
・Windows通知表示
・共有フォルダの作成/追記権限

1台PCでは250workerの競合負荷試験までクリア済みの設計を
本番フォーム側へ反映したバージョンです。


■ ファイル構成

README.txt
mention-form.config.ini
mention-form.css
mention-form.js
mention-request-worker.ps1
メンション依頼フォーム.hta


[v2 設定パス判定修正]
--------------------
v1ではCSV_FOLDERの値に「【」が含まれているだけで
未設定と誤判定する不具合がありました。

例：
C:\Users\tagut\OneDrive\デスクトップ\【拡張機能】\HTA\書き込み用

上記のような正常なパスも未設定扱いになっていました。

v2では、
CSV_FOLDER=【ここに共有フォルダのUNCパスを設定】
という初期プレースホルダーの場合だけ未設定扱いにします。

日本語・【】を含むフォルダ名は正常に使用できます。


[v3 送信完了メッセージ変更]
--------------------------
フォーム上の受付メッセージを以下へ変更しました。

タイトル：
送信完了

本文：
メンション依頼を送信しました。

受け付け完了後、右下のWindows通知でお知らせいたします。

Background Worker / Pending / lock / 再試行 / 重複防止などの処理仕様は変更ありません。


[v4 本番環境UNC起動対応]
------------------------
本番環境でHTA自体を共有フォルダ（UNCパス）から起動した場合、
window.location.pathname だけではサーバー名が取得できず、
mention-form.config.ini の検索先が誤る可能性がありました。

例：
実際：
\\server\share\HTA\メンション依頼フォーム.hta

旧判定：
\share\HTA
→ server名を欠落
→ iniが見つからない

v4：
location.hostname と pathname を組み合わせて
\\server\share\HTA
を正しく復元します。

ローカルCドライブ起動も従来どおり対応します。

また、iniが見つからない場合は
「確認先：xxxx」
として、実際に探したフルパスをエラーへ表示します。


[v5]
- 各依頼にクリアボタンを追加
- 組織＋CA名を常時必須へ変更
- 代理CA組織／代理CA名は片方入力時のみ相互必須
- 依頼2→依頼1、依頼3→依頼2の同一CA反映チェックを追加
- チェック中は反映先を編集不可、参照元変更に追従
- チェック解除後は値を残したままフリー入力へ切替


[v6 同一CAチェック解除時の安全化]
--------------------------------
同一CAチェックをOFFにしてフリー入力へ切り替える際、
以下の反映済み項目を自動クリアする仕様へ変更しました。

・組織
・CA名
・代理CA組織
・代理CA名1
・代理CA名2
・代理CA名3

これにより、同一CAチェック解除後に前の依頼内容が残ったまま送信してしまうリスクを抑えます。
メールメモ / 処理日時 / 期日 / タイプ / 時短 / 至急はクリアしません。


[v7 配置・サイズ統一]
・クリアボタンを各カード右下へ移動
・クリアボタンは既存の「2件目を入力 / 3件目を入力」と同じ sub-button を使用
・同一CAチェックを依頼2 / 3 の「組織」直下へ移動
・同一CAチェックは既存の「時短 / 至急」と同じ checkline を使用
・機能仕様は変更なし


[v8 操作ボタン横並び]
--------------------
・カード下部の操作ボタンを1列に集約
・依頼1：2件目を入力 → クリア
・依頼2：3件目を入力 → 取り消し → クリア
・依頼3：取り消し → クリア
・既存のボタン色 / サイズ / 機能は維持


[v9 本番環境対策｜ini廃止＋再送機能]
=====================================

■ mention-form.config.ini を廃止
CSV保存先は mention-form.js 冒頭の
PRODUCTION_CSV_FOLDER
へ管理者が1回だけ設定する方式へ変更しました。

利用者側でiniやフォルダを準備する必要はありません。

※この試作版は以下が設定待ちです。
PRODUCTION_CSV_FOLDER = "【本番CSV保存先のパスをここに設定】"

■ Pending退避
送信時にデスクトップへ
MentionRequest_Pending
フォルダを自動作成します。

依頼ごとに .pending ファイルを作成し、
CSV書き込み成功後は従来どおり
「成功したPendingファイルだけ」を削除します。

Pendingフォルダが空になった場合は、
フォルダ自体も自動削除します。

■ ×でフォームを閉じた場合
送信受付後はBackground Workerが別プロセスで動作するため、
フォームを×で閉じてもCSV書き込みと自動再送を継続します。

■ 自動再送
共有CSVへ書き込めない場合は最大300秒（5分）自動再送します。

300秒経過しても成功しない場合：
・Pendingは削除しない
・Windows通知「メンション依頼｜未送信」を表示
・Workerは終了

■ 次回起動時の再送
フォーム起動時にDesktop\MentionRequest_Pendingを確認し、
未送信Pendingがあれば自動再送します。

■ 手動再送
未送信Pendingが存在する場合のみ、
フォーム下部に

⚠ 未送信 n件
［未送信を再送］

を表示します。

ボタン押下で残存Pendingを再送できます。

■ 重複防止
従来のRequestID＋依頼番号による重複確認、
CSV lock、途中書き込み補完は維持しています。

■ ファイル構成
README.txt
mention-form.css
mention-form.js
mention-request-worker.ps1
メンション依頼フォーム.hta

mention-form.config.ini は不要です。


[v10 自動再送時間延長]
----------------------
大量同時書き込み時のロック待ちを考慮し、
Background Workerの最大自動再送時間を

60秒 → 300秒（5分）

へ延長しました。

300秒以内に書き込み成功：
・通常どおりPendingを削除
・成功通知

300秒経過しても書き込めない場合：
・Pendingを残す
・Windows通知「メンション依頼｜未送信」
・Worker終了
・次回フォーム起動時の自動再送 / 手動再送が可能


[v11 送信状態表示の整理]
------------------------
■ 通常
［送信］ボタンを表示します。

■ 送信ボタン押下後
・［送信］ボタンを非表示
・再送ボタンも非表示
・フォーム下部に以下のメッセージだけ表示

  送信中です… 完了後、Windows通知でお知らせします。

CSV書き込み成功後はPendingが削除され、
フォームを開いたままの場合は通常表示へ戻ります。

■ 自動再送中
再送ボタンは表示せず、

  未送信データを自動再送中です…

のみ表示します。

■ 300秒経過しても送信できない場合
Pendingを残したまま、

  ⚠ 送信できませんでした｜未送信 n件
  ［未送信を再送］

を表示します。

■ 手動再送
［未送信を再送］押下後はボタンを非表示にして、

  再送中です… 完了後、Windows通知でお知らせします。

のみ表示します。

再送成功：通常表示へ戻る
再送失敗（300秒）：再送ボタン＋失敗メッセージを再表示

Background Worker / Pending / lock / 重複防止の処理仕様は変更ありません。


[v12 送信ボタン常時表示]
------------------------
新規依頼を続けて送信できるよう、
送信ボタンはPending / 自動再送 / 手動再送 / 送信失敗の状態に関係なく
常に表示する仕様へ変更しました。

■ 送信中
  送信中です… 完了後、Windows通知でお知らせします。
  ［送信］

■ 自動再送中
  未送信データを自動再送中です…
  ［送信］

■ 送信失敗
  ⚠ 送信できませんでした｜未送信 n件
  ［未送信を再送］ ［送信］

■ 手動再送中
  再送中です… 完了後、Windows通知でお知らせします。
  ［送信］

未送信データの処理中でも、
別の新規依頼を続けて送信できます。

Background Worker / Pending / 300秒自動再送 /
CSV lock / 重複防止の処理仕様は変更ありません。


[v13 24インチ表示・左右余白補正]
--------------------------------
24インチモニターやWindows表示倍率の違いで、
フォーム左右の余白が広すぎる / 狭すぎるように見える問題へ対策しました。

■ 変更内容
・#app の左右余白を auto から 14px固定へ変更
・フォーム全幅の計算へ左右余白28pxを含める
・HTA枠幅を可能な環境では実測
・resizeTo後に実際のviewportを再測定
・数pxのズレがあれば1回だけ自動補正
・補正後も画面中央へ再配置

■ 目的
1920×1080の24インチモニター、
Windows表示倍率100% / 125%などでも、
左右の余白ができるだけ均等に見えるようにします。

フォーム内部の入力欄・カード・ボタン配置・機能仕様は変更していません。


[v14 CSV保存先：HTAと同じ階層の「書き込み用」フォルダ]
-----------------------------------------------------
CSV保存先の固定パス設定を廃止しました。

■ 配置例
メンション依頼フォーム/
├─ メンション依頼フォーム.hta
├─ mention-form.js
├─ mention-form.css
├─ mention-request-worker.ps1
└─ 書き込み用/
   └─ 拠点_YYYYMMDD.csv

■ 動作
・HTA自身の配置場所を取得
・同じ階層の「書き込み用」フォルダをCSV保存先として使用
・PCごとの C:\Users\... 設定は不要
・mention-form.config.ini も不要
・「書き込み用」フォルダが存在しない場合は送信せずエラー表示

■ 注意
共有ディスク上で使用する場合、
「書き込み用」フォルダに利用者の書き込み権限が必要です。

Pendingは従来どおり各PCのデスクトップへ退避します。
Background Worker / 300秒再送 / CSV lock / 重複防止仕様は維持しています。


[v15 1920×1080表示の安定化]
----------------------------
24インチ / 1920×1080 / Windows表示倍率100%で、
フォームが必要以上に縮小される問題を修正しました。

■ 変更前
1920×1080でも comfortScale により約96.5%へ強制縮小。

■ 変更後
・画面に100%で収まる場合は縮小しない
・1920×1080ではフォーム本来のサイズを維持
・ウィンドウは画面中央へ配置
・左右の画面余白を均等化
・画面が狭い場合のみ自動縮小
・resize後の実測補正は従来どおり維持

CSV保存先はv14同様、
HTAと同じ階層の「書き込み用」フォルダです。


[v16 3件表示時の横幅安定化]
----------------------------
1920×1080 / 100%環境で3件表示した際、
高さに合わせた縮小率がHTAの横幅にも適用され、
右側項目が切れる・右側に不自然な余白が残る問題を修正しました。

■ v16の考え方
・縦方向：3件が画面に収まるよう従来どおり自動縮小
・横方向：1920×1080で余裕がある場合はウィンドウ幅を縮めない
・縮小されたフォームは左右中央へ配置
・1件 / 2件 / 3件で横幅の見た目を安定させる
・狭い画面では従来どおり横方向にも自動フィット

CSV保存先はv14以降と同様、
HTAと同じ階層の「書き込み用」フォルダです。
300秒再送・Pending・重複防止仕様も維持しています。


[v17 3件表示時の右余白対策]
----------------------------
3件表示で縮小した際、
フォーム本体よりHTAウィンドウの横幅だけ広く残り、
右側に大きな余白が発生する問題へ対策しました。

■ v17の変更
・縮小後の #app の実描画幅を getBoundingClientRect で実測
・その実幅 + 左右14px をHTAの目標幅として使用
・左右余白は14px固定
・ウィンドウ自体を画面中央へ配置
・1件 / 2件 / 3件で、必要以上の右余白を作らない

従来の機能
・同階層「書き込み用」フォルダ
・Desktop Pending
・300秒再送
・重複防止
は維持しています。


[v18 右余白対策：resizeToとzoomの処理順修正]
---------------------------------------------
3件表示時に右側の余白が残る問題について、
HTA / IE環境で body zoom 適用後の window.resizeTo が
zoom座標の影響を受けている可能性へ対策しました。

■ v18の処理順
1. body zoom = 100% に戻す
2. フォーム本来の幅・高さを測定
3. 必要な縮小率を計算
4. zoom=100%の状態のまま window.resizeTo
5. ウィンドウサイズ確定後にzoomを適用
6. 描画後、右側に10px以上の余白が残れば1回だけ実測補正

■ 維持機能
・HTAと同じ階層の「書き込み用」フォルダ
・Desktop Pending
・300秒再送
・CSVロック / 重複防止
・送信ボタン常時表示


[v19 カード固定サイズ / 自動縮小廃止]
------------------------------------
24インチモニターでの表示安定性を優先し、
フォーム全体の自動zoom調整を廃止しました。

■ v19の仕様
・依頼1件 / 2件 / 3件でカードサイズは変わりません
・文字サイズ、入力欄サイズ、ボタンサイズも変わりません
・body zoom は常に100%
・横幅は1680px固定
・1920×1080では左右に余裕を残して表示
・3件表示で縦に収まらない場合のみ縦スクロール
・表示件数の変更時もカードそのものは縮小しません

■ 維持機能
・HTAと同じ階層の「書き込み用」フォルダ
・Desktop Pending
・300秒再送
・CSVロック / 重複防止
・送信ボタン常時表示


[v20 24インチ固定コンパクト / スクロールなし]
---------------------------------------------
v19の「自動縮小なし・カードサイズ固定」を維持したまま、
縦方向だけ全体を少しコンパクトにしました。

■ v20
・依頼1 / 2 / 3でカードサイズは同一
・自動zoomなし
・横幅1680px固定
・カード高238px固定
・入力欄高40px
・カード内の上下余白を圧縮
・カード間隔を9pxへ縮小
・フッター高を約78pxへ圧縮
・縦スクロールバーは表示しない
・1920×1080 / 表示倍率100%で3件＋フッターを収める想定

■ 維持
・同階層「書き込み用」フォルダ
・Desktop Pending
・300秒再送
・CSVロック / 重複防止
・送信ボタン常時表示


[v21 操作ボタン切れ対策]
------------------------
v20のコンパクト化により、
「クリア」「この依頼を取り消す」などの操作ボタンが
カード下端に食い込んで一部隠れる問題を修正しました。

■ v21
・カード高さ238pxは維持
・自動縮小なし
・スクロールバーなし
・操作ボタン列をカード下端から10pxの位置へ固定
・依頼1 / 2 / 3すべて同じ位置ルール
・「2件目を入力」「3件目を入力」が非表示になった場合も
  残ったボタンは右寄せのまま表示

その他の機能仕様はv20から変更ありません。
